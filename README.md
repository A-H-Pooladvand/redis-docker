# Redis Docker

The Redis docker project

## Getting started
- [Quick start](#quick-start)

### Quick Start
- Copy `.env.example` file to .env
- Set `.env` variables
- Run `docker-compose up -d --build`

**To list all available Makefile commands please run**

```shell
make help
```